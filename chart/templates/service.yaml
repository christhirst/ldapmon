apiVersion: v1
kind: Service
metadata:
  name: {{ include "ldapmon.fullname" . }}
  labels:
    {{- include "ldapmon.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "ldapmon.selectorLabels" . | nindent 4 }}
